{
    "name": "ts-swc",
    "version": "1.0.0",
    "description": "TypeScript project with SWC compiler",
    "main": "dist/index.js",
    "scripts": {
        "build": "swc src -d dist",
        "dev": "swc src -d dist --watch",
        "start": "node dist/index.js",
        "clean": "rm -rf dist"
    },
    "devDependencies": {
        "@swc/cli": "^0.1.62",
        "@swc/core": "^1.3.78",
        "@types/node": "^20.5.0",
        "typescript": "^5.1.6"
    }
}
